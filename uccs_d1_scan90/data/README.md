# data/

`uccs_d1_scan90` の実機ログ（RX/TXSD のSDカード内容）をここに集約する。

## ルール

* 1回の計測ごとに日付ディレクトリを切る（例: `2025-12-14_run01/`）。
* その中に `RX/` と `TXSD/` を作り、SDの `/logs/` を丸ごとコピーする。
* 追加で `notes.md` に測定条件（距離/環境/電源/scan duty 等）を短く残す。

