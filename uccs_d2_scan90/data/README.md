# data/

`uccs_d2_scan90` の実機ログ（RX/TXSDのSDカード内容）をここに集約する。

## ルール

* 1回の計測ごとに run ディレクトリを作成（例: `01/`, `02/`）。
* その中に `RX/` と `TXSD/` を作り、SDの `/logs/` を丸ごとコピーする。
* `README.md` に測定条件（距離/環境/電源/scan duty 等）を簡潔に残す。

