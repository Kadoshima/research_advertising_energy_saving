2026-01-16 (JST)

- 変更: `esp32_firmware/20260114_deltae_v3rig/RX_DeltaE_V3/RX_DeltaE_V3.ino`
  - RX(V3) の MFD 解析を強化：メーカー広告が「company_id付き」やバイナリでも、ペイロード内を探索して `MFxxxx` を抽出（`MFxxxx` が先頭に来ないケースでもrx=0にならない）。
  - NimBLE / 非NimBLE の両分岐で `cbTotal / mfdFail / addrMis / bufDrop / firstAddr / firstMfd` 診断が出るよう統一。
  - 周期DBG（DBG_LEVEL>=3）を「状態変化時 or 10秒ごと」に抑制。
  - 未使用の SYNC 割り込み関連（`syncLvl` / `onSync()`）を削除。

- 変更: `esp32_firmware/20260114_deltae_v3rig/TXSD_DeltaE_V3_ON/TXSD_DeltaE_V3_ON.ino`
  - 未使用の `nextPath()` を削除（サイズ/混乱の低減）。
  - 周期DBG（DBG_LEVEL>=3）を「状態変化時 or 10秒ごと」に抑制。

- 変更: `esp32_firmware/20260114_deltae_v3rig/TXSD_DeltaE_V3_OFF/TXSD_DeltaE_V3_OFF.ino`
  - 周期DBG（DBG_LEVEL>=3）を「状態変化時 or 10秒ごと」に抑制。

- 追加: `scripts/sweep_status.py`
  - TXのシリアル無しで、RX/TXSDのCSV（SDカード `/logs/` のコピー）から sweep の完走可否を判定するスクリプトを追加。
  - 判定指標: ファイル数（expected 50）、start_ms逆行（周回/リセット疑い）、RXの受信間隔推定、TXSDのadv_count健全性。

- 追記: `esp32_firmware/20260115_deltae_v3rig_sweep/README.md`
  - 「完了判定（TXのシリアル無し）」の手順とコマンドを追記。

- 変更: `esp32_firmware/20260115_deltae_v3rig_sweep/RX_DeltaE_Sweep/RX_DeltaE_Sweep.ino`
  - MFD 解析を「ペイロード内の `MFxxxx` 探索」に変更（company_id前置き等に耐性）。
  - 周期DBG（DBG_LEVEL>=3）を「状態変化時 or 10秒ごと」に抑制。

- 変更: `esp32/RX_BLE_to_SD_SYNC_B.ino`
  - 実運用向けに安定化パッチを適用（dual SYNC入力、polling+debounce、`SD.exists()`走査の撤去、CSVに`prog_id`列追加、RX診断カウンタ追加、MFDをペイロード探索で抽出）。

- 変更: `esp32/TXSD_PowerLogger_SYNC_TICK_OFF.ino`, `esp32/TXSD_PowerLogger_SYNC_TICK_ON.ino`
  - dual SYNC入力で開始を安定化（polling+debounce）。終了は従来互換の固定窓（fallback）を維持。
  - CSVに`prog_id`列を追加し、行にも`prog_id`を付与（後段のデータ整理を容易化）。
